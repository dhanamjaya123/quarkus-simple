deployment:
    image: quay.io/dhanamjaya/do400-deploying-environments
    version: 1.2.0
    replicas: 1
    includeHealthChecks: true

route:
    host: simple-article-helm.apps-crc.testing

config:
    greeting: 'We are on a newer version now!'

